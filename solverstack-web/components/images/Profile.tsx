import React, { ReactElement } from "react";

// Bootstrap
import Image from "react-bootstrap/Image";

const Profile = (): ReactElement => {
    return <Image src="/profile_unknown.png" roundedCircle height="250px" />;
};

export default Profile;
